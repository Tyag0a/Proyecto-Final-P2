package co.edu.uniquindio.marketplace.mapping.dto;

public record ProductoDto(
                          String nombre,
                          String imagen,
                          String categoria,
                          double precio) {
}
